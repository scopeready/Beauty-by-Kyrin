{
  "//": "Type checking for the booking API via JSDoc. Scoped to api/ on purpose: that is the code where a type error loses a lead. The generator and templates are covered far more thoroughly by verify.mjs, which asserts against the real built HTML. No emit — tsc is only a checker.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "noImplicitAny": false,
    "strictNullChecks": true,
    "noUnusedLocals": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["api/**/*.mjs"],
  "exclude": ["node_modules", "dist", ".build"]
}
